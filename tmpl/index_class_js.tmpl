import React, { Component } from 'react';
import PropTypes from 'prop-types';
import style from './<%= componentDashName %>.css';

export class <%= componentName %> extends Component {
    static propTypes = {
        
    }

    static defaultProps = {
        
    }

    state = {
        
    }

    render() {
        return (

        );
    }
}

