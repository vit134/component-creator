import React, { Component } from 'react';
import PropTypes from 'prop-types';
import style from './<%= stylesName %>.css';

export class <%= name %> extends Component {
    static propTypes = {
        
    }

    static defaultProps = {
        
    }

    state = {
        
    }

    render() {
        return (

        );
    }
}

